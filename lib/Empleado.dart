// Clase que representa un empleado
class Empleado {
  int id; // Identificador único del empleado.
  String nombre; // Nombre del empleado.
  
  Empleado(this.id, this.nombre); // Constructor que inicializa los atributos id y nombre.
}